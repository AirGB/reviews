config:
  target: "http://localhost:3002"
  phases:
    - duration: 60
      arrivalRate: 20
  variables:
    listingid:
      - [9999]
scenarios:
  - flow:
    - loop:
      - get:
          url: "/api/listing/{{$loopElement}}/overview"
      over: listingid
